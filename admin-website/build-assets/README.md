# Desktop build assets

`electron-builder` uses this directory for Windows installer resources. Add a
Windows `.ico` file here and reference it from `electron/electron-builder.yml`
when an approved application icon is available.
