// The UI is a client-side application shell. This catch-all lets documented
// routes (for example /dashboard and /admin/alerts) resolve on a direct
// browser request while preserving the Next API route tree.
export { default } from "../page";
